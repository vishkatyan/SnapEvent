# SnapEvent - An Event Organizer

This is an event organizer platform where a number of events are listed and users can register for particular event. 
The UI is build on React, interactive and responsive.
Backend is written on Node and the DB used here is Mongo Db.
Postman is used for APi testing.
